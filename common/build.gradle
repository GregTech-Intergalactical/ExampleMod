archivesBaseName = "${rootProject.archive_base_name}-common"
version = "${rootProject.minecraft_version}-${project.mod_version}"

dependencies {
    //modCompileOnly("com.github.GregTech-Intergalactical.AntimatterAPI:antimatter-common:${antimatter_version}")
}

architectury {
    common("forge", "fabric")
}